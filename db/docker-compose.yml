version: "3.1"

services:
  mysql-dev:
    image: mysql:8.0.15
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: radio_db
    ports:
      - "3308:3306"
    volumes:
      - ./data:/docker-entrypoint-initdb.d